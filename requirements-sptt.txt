# Узел с камерой SPTT (CSDU-429). Диапазоны версий — под CentOS 7.8 / Python 3.11.7.
-r requirements.txt

pyusb>=1.2,<2          # чистый Python, нужна системная libusb-1.0
astropy>=5.3,<6.2      # запись FITS; 7.x требует numpy 2.x
