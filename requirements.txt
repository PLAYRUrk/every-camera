# Common
numpy>=1.20
paho-mqtt>=1.6

# GUI (optional — only needed for --gui mode)
PyQt5>=5.15

# Canon camera (gphoto2)
gphoto2-cffi>=0.4
Pillow>=9.0

# SPTT camera (CSDU-429)
pyusb>=1.2
astropy>=5.0

# Infra camera (SW1300)
# Python deps: numpy (already above), ctypes (stdlib)
# System deps (install via apt):
#   sudo apt install libusb-1.0-0 libqt5gui5 libqt5core5a libgl1-mesa-glx
