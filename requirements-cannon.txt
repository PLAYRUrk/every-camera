# Узел с камерой Canon (gphoto2). Диапазоны версий — под CentOS 7.8 / Python 3.11.7.
-r requirements.txt

# Колёс не публикует: собирается из исходников. Нужны системные пакеты
#     sudo yum install gcc libffi-devel libgphoto2-devel
gphoto2-cffi>=0.4,<1.0
